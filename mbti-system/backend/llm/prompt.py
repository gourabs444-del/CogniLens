TRAIT_JSON_EXAMPLE = "{}"


def build_trait_prompt(answer):
  return "MBTI signal extraction prompt is not configured."


def build_result_prompt(result):
  return "MBTI result prompt is not configured."
